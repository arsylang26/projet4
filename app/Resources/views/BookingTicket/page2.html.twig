{% extends "::layout.html.twig" %}
{% block body %}
<h3>Formulaire de réservation des billets du Musée du louvre</h3>
<div class="well">
    {{ dump(form) }}
    {{ form_start(form) }}
    {{ form_row(form) }}
    <input type="submit"/>
    {{ form_end(form) }}
</div>
{% endblock %}
